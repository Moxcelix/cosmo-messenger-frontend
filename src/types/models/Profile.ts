export interface Profile {
    id: string
    user_id: string
    username: string
    email: string
    display_name: string
    avatar_url: string
    bio: string
    is_active: boolean
    is_deleted: boolean
    created_at: string
}